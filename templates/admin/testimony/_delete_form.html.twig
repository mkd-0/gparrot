<form method="post" action="{{ path('app_testimony_delete', {'id': testimony.id}) }}" onsubmit="return confirm('Êtes-vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testimony.id) }}">
    <button class="btn btn-danger ms-5">Supprimer</button>
</form>
