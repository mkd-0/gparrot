{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
<h1 class="mx-5">{{ brand.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_brand_list') }}" class="btn btn-secondary mb-2 ms-5">Retour</a>

    <a href="{{ path('app_brand_edit', {'id': brand.id}) }}" class="btn btn-warning mb-2">Modifier</a>

    {{ include('admin/brand/_delete_form.html.twig') }}
{% endblock %}
