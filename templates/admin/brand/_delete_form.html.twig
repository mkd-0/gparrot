<form method="post" action="{{ path('app_brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Êtes-vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn btn-danger ms-5">Supprimer</button>
</form>
