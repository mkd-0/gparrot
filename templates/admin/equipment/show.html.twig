{% extends 'base.html.twig' %}

{% block title %}Equipment{% endblock %}

{% block body %}
<h1 class="mx-5">Equipment : {{ equipment.name }} </h1>
<div class="ms-5 w-75">
    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ equipment.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipment.name }}</td>
            </tr>
        </tbody>
    </table>
</div>
    <a href="{{ path('app_equipment_list') }}" class="btn btn-secondary mb-2 ms-5">Retour</a>

    <a href="{{ path('app_equipment_edit', {'id': equipment.id}) }}" class="btn btn-warning mb-2">Modifier</a>

    {{ include('admin/equipment/_delete_form.html.twig') }}
{% endblock %}
