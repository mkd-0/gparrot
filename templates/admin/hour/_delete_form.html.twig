<form method="post" action="{{ path('app_hour_delete', {'id': hour.id}) }}"  onsubmit="return confirm('Êtes-vous sûr?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hour.id) }}">
    <button class="btn btn-danger ms-5">Supprimer</button>
</form>
