<nav class="navbar sticky-top navbar-expand-lg bg-custom-nav ">
  <div class="container-fluid">
    <a class="navbar-brand">
      <img src="{{asset('img/logo_vparrot.png')}}" alt="Garage Parrot"  height="30px"/> 
    </a>
   
    {# note: responsive btn #}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>


    <div class="collapse navbar-collapse d-flex justify-content-start" id="navbarSupportedContent">
      <!-- Trois premiers liens à gauche -->
      <ul class="navbar-nav ">
        <li class="nav-item">
          <a class="nav-link text-white active" aria-current="page" href="{{path('app_home')}}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{path('app_occasion')}}">Nos occasions</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{path('app_contact')}}">Contactez-nous</a>
        </li>                    
      </ul>
    </div>

  
      <!-- Quatrième lien à droite -->
      <div class="d-flex justify-content-end">
        <ul class="navbar-nav">  
          
          {% if app.user %}
          <li class="nav-item">
            <a href="{{path('app_config')}}" class="btn btn-info me-5">Administration</a>
          </li>
          <li class="nav-item">
            <a href="{{path('app_logout')}}" class="btn btn-danger" >Me déconnecter</a>
          </li>
          {% else %}
          <li class="nav-item">
            <a href="{{path('app_login')}}" class="btn btn-info">Administration</a>
          </li>
       
          {% endif %}
          
        </ul>
      </div > 
  </div>
</nav>
  